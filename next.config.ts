/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['media.istockphoto.com', 'developers.google.com', 'lh3.googleusercontent.com'],
  },
};

module.exports = nextConfig;
